import React from "react";

const AppFormLoader = ({ css }) => {
  return (
    <div className="flex justify-center">
      <div className={css}>
        <img src="/formLoader2.svg" alt="" />
      </div>
    </div>
  );
};

export default AppFormLoader;
